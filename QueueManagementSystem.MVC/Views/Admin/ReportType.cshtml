@{
    ViewData["Title"] = "Dashboard";

}




<div class="text-center">
    <h1 class="dashboard-title">Reports Dashboard</h1>

    <div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-5">
    <!-- Card 1: Configure Reports -->
    <div class="col">
        <a href="@Url.Action("Index", "Reports")" class="text-decoration-none">
            <div class="dashboard-card text-center">
                <div class="card-body">
                    <div class="mb-3">
                        <i class="fas fa-file fa-2x"></i>
                    </div>
                    <h5 class="card-title">Reports</h5>
                </div>
            </div>
        </a>
    </div>

    <!-- Card 2: Configure Alaytics  -->
  @*   <div class="col">
        <a href="@Url.Action("Analytics", "Admin")" class="text-decoration-none">
            <div class="dashboard-card text-center">
                <div class="card-body">
                    <div class="mb-3">
                        <i class="fas fa-chart-bar fa-2x"></i>
                    </div>
                    <h5 class="card-title">Analytics</h5>
                </div>
            </div>
        </a>
    </div> *@

    </div>
</div>



<style>
    .dashboard-title {
        color: #004d4d;
        font-weight: bold;
        font-size: 2rem;
        padding: 15px 0;
        margin-bottom: 30px;
        border-bottom: 2px solid rgba(0, 102, 102, 0.2);
    }

    .dashboard-card {
        background-color: #FFFFFF;
        color: #006666;
        border-radius: 12px;
        padding: 20px;
        height: 100%;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

        .dashboard-card:hover {
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
            transform: translateY(-5px);
        }

        .dashboard-card i {
            color: #00A3A3;
        }

        .dashboard-card h5 {
            font-weight: 600;
            margin-top: 10px;
        }

    @@media (max-width: 767px) {
        .dashboard-title {
            font-size: 1.5rem;
        }

        .dashboard-card {
            padding: 15px;
        }
    }
</style>